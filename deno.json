{
  "tasks": {
    "dev": "deno run --watch main.ts",
    "example": "mkdir -p output && deno run --allow-read --allow-write main.ts example.txt -o output/example.svg"
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@1"
  }
}
